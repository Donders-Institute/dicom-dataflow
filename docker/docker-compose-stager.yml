version: "2"
services:
  redis:
    image: redis
    command: redis-server --appendonly yes
    network_mode: "host"
    volumes:
      - /scratch/data_stager:/data
  dccn-dirdm-irodsclient:
    build:
      context: ./irodsclient
    image: dccn-dirdm-irodsclient
  dccn-dirdm-stager:
    build: ./stager
    image: dccn-dirdm-stager
    network_mode: "host"
    volumes:
      - /project:/project
      - ./irods_otp:/opt/stager/irods_otp:ro
    depends_on:
      - dccn-dirdm-irodsclient
      - redis
